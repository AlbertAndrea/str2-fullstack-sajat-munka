const utils = require('./utils')

const priceList = [555, 456, 777, 666, 765, 24, 66]

utils.even = 2
utils.odd = 1

console.log(utils)
console.log(utils.even(priceList))

utils.users.age = 18

console.log(utils.users);